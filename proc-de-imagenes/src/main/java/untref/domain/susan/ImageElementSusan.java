package untref.domain.susan;

import javafx.scene.paint.Color;

public interface ImageElementSusan {
	Color calculateElement(double elementDetectionParameter, Double accumulateDelta, Color imageColor);
}
